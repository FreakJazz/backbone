# Copy to .env before `python main.py`. Defaults match docker-compose.yml
# at examples/docker-compose.yml.

POSTGRES_DSN=postgresql://backbone:backbone@localhost:5433/backbone_products
MONGO_URI=mongodb://backbone:backbone@localhost:27018
MONGO_DB=backbone_transactions
PORT=5000
