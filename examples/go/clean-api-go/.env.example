# Copy to .env (or export directly) before `go run main.go`.
# Defaults match docker-compose.yml at examples/docker-compose.yml.

POSTGRES_DSN=postgres://backbone:backbone@localhost:5433/backbone_products?sslmode=disable
MONGO_URI=mongodb://backbone:backbone@localhost:27018
MONGO_DB=backbone_transactions
PORT=8005
